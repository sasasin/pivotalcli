require "spec_helper"

describe Pivotalcli do
  it "has a version number" do
    expect(Pivotalcli::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
